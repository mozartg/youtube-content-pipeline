# THREAD_HANDOFF_SENDDESK-0626.md — Persistent Handoff / Prescription
**Lane:** changeby Nonprofit Consulting — Deployment & Last-Mile (within the Machine Floor / ARBNET lineage).
**Written:** 2026-06-26, as a deliberate persistence artifact — assume the originating thread is deleted. Companion to `rx.md` (Plumbline / Web-SMB lane); does not overwrite it.

## 0. PROPOSED NAME: **Throughline**
A throughline is the line that carries work *through* to the finish — and the connecting thread that carries a contribution *through* deletion. This lane's function: take fully-built assets and force the last mile (the send), attack the deployment bottleneck, reconcile contradictions, and leave a record that survives context loss. Sub-identity INSIDE the canon; does NOT replace resume code `ARBNET-0618-C1` or the voice countersign. Alts: Linchpin, Capstone.

## 1. REASONING STYLE (model-agnostic — carry forward regardless of who runs it)
The behaviors that made this thread productive. No model names; these are operating standards, not a tier.
1. **Anti-recency EV selection.** Priority = (Expected $ × P(complete today)) / Mo's time-to-approve. Do NOT build the most recent or most salient thing; rank the whole portfolio and work the top item to finished.
2. **Verify-before-build.** Inventory what already exists before producing anything. Refuse duplicates. (This thread found the entire funnel already built and therefore did NOT build a 7th asset.)
3. **Attack the real bottleneck, not the easy one.** Production was done; the binding constraint was the click (the send). Build the thing that removes send-friction, not more inventory.
4. **Evidence over assertion.** Web-verify every present-day fact (names, prices, contacts) against a live source, with a date. Never assert from memory. (This thread caught a wrong recipient name before Mo sent to it.)
5. **Reconcile contradictions; don't average them.** When two records conflict, surface it and recommend — don't quietly split the difference.
6. **Separate facts / assumptions / recommendation** in every output.
7. **Draft-only. Mo is last-mile.** Never send, spend, move money, place a bet, or reset a password. Stage; he executes.
8. **End every task with exactly one last-mile action** (≤15 min for Mo).
9. **Honest about missing tools/access.** Stage so it fires when unblocked; never fake "done." (E.g., flag when an email isn't public or a push needs auth.)
10. **Refuse rationalized busywork and say so.** Bar: 3 hrs of Mo's action → ≥$1/day realized or pipeline. A new asset on top of 170 undeployed ones is busywork; getting one sent is not.

## 2. SELF-TEST FOR THE INCOMING THREAD (a behavior test, not a trivia quiz — pass ≥7/8 before producing, then post the countersign)
Designed so a thread of ANY capability can check whether it is operating at the bar:
1. Before building, did I inventory what already exists and confirm I'm not duplicating? (If I can't name what's already built, I haven't oriented.)
2. Did I pick the work by EV — expected $ × P(complete) / time-to-approve — not by what was mentioned most recently?
3. Is my output a FINISHED artifact someone can act on, or just analysis? (Analysis-only = fail.)
4. Did I verify every present-day fact against a live source with a date?
5. When two docs conflicted, did I flag it rather than silently averaging?
6. Did I keep everything draft-only — no send/spend/password actions?
7. Does the task end with exactly ONE last-mile action ≤15 min for Mo?
8. Honest line: "Is what I'm about to do needle-moving, or busywork I'm rationalizing?"
**Concrete probe:** "What is the single highest-EV UNBUILT asset in the active lane right now, and why?" If you cannot answer from the record, re-orient before producing.
**Canon proof:** a correctly-loaded thread ends its first reply with — `Machine stages, Mozart places — ARBNET C1 online.`

## 3. CANONICAL ANCHORS (reuse; do not re-derive — inherits rx.md §3)
- Resume `ARBNET-0618-C1`; countersign "Machine stages, Mozart places — ARBNET C1 online."
- Board of record: **ClickUp** (team 20122996). changeby pipeline list = **901417511645**. **TickTick** = personal-dev SOPs only. **NOT Todoist.**
- **Positioning (use everywhere):** Mo = a social worker (MSW) who understands the clinical/nonprofit space + a tech & automation expert. NOT "therapist," NOT "developer." Real anchors: Executive Director of 21 Progress (donor base 20→~300, ~14x new revenue), City of Rochester Deputy Chief of Community Engagement, NWIRP secure case-management build, Meridian CRM migration. **Faith = background values anchor, NOT the commercial lead.** Client tone = professional, concise, not familiar.
- **Pricing standard:** changeby service ladder — **$350 Readiness Audit → $500–1,500 single grant → $800–2,000/mo retainer** (audit carries a "$5K-reachable-or-free" guarantee). Web/SMB lane uses $300/$600/$800 (per rx.md).
- **Hard boundaries (constraints.yaml):** money_class 3 (account actions) = manual only. Never send/buy/move-money/reset-passwords without explicit go. BNPL never funds bets.

## 4. WHAT THIS THREAD DID (live state of the changeby / Grant-Ready line)
- **Diagnosis (evidence-based):** the "Grant-Ready Nonprofit" funnel is FULLY BUILT — lead magnet ("12 Things Funders Look For" PDF), 5-email course, $49 toolkit (xlsx + docx), sales page + Gumroad/Etsy listing, the $350 service offer + Readiness-Audit leave-behind PDF, lead tracker, and outreach drafts. The 6/24 retirement packet and 6/24 scorecard agree: $0 over 8+ days, 0 of 170+ assets deployed. **The bottleneck is the click, not production.**
- **Built this run:** the **changeby Send Desk** — a verified, consolidated, ready-to-send outreach packet for the #1 corrected-EV play (the $350 Readiness Audit), so Mo's last mile collapses to ~15 minutes.
- **Verified 4 targets (2026-06-26, live sources):**
  - Refugees Helping Refugees → **Mai Abdullah** · rhr@rhrroc.org (verified)
  - Keeping Our Promise → **Ellen Smith** · Ellen@KeepingOurPromise.org (verified)
  - Mary's Place Refugee Outreach → **Charlsey Bickett** · charlsey.bickett@marysplaceoutreach.org (sourced; alt @dor.org)
  - Rochester Refugee Resettlement Svcs → **Djifa Kothor** · email NOT public (call 585-319-3134)
- **Send-blocking error caught:** Gmail drafts (incl. one dated 6/26) addressed RHR to "Sadiya" — stale. Current ED is Mai Abdullah. Fix before sending.
- **Contradiction flagged:** the $49 toolkit was retired 6/24 (nonprofits buy done-for-you/software, not DIY; big CRMs give templates free) yet still has an open ClickUp PUBLISH task (86bakk1kw) and a 6/25 sales page. Recommendation: lead with the $350 service; demote the toolkit to an order-bump/leave-behind.

## 5. ASSET GLOSSARY + COUNT (this thread — 7 net-new assets)
1. **changeby_SendDesk_2026-06-26.docx** — action card + 4 paste-ready, corrected emails + Gmail-draft cleanup + toolkit decision + sources. Locations: Cowork outputs; `C:\Users\mozar\Documents\WorkerFloor\GrantReady_Toolkit\`.
2. **changeby_SendDesk_Tracker_2026-06-26.xlsx** — verified send tracker (org, director, email, verification, subject, action, status). Same two locations.
3. **Drive record** — Google Doc "changeby Send Desk — Verified & Ready to Send (2026-06-26)", id `1r-vWwWb7M9qSrZatjWzLXMjm-WnUspx99Zc1Ue4Zslo`.
4. **ClickUp comment** on send task `86bam5jmy` (verified send-desk summary), comment id `90140220119160`.
5. **THREAD_HANDOFF_SENDDESK-0626.md** (this file) — workspace + Cowork outputs + Drive + git.
6. **rx.md appended section** (additive; Plumbline's content untouched).
7. **ClickUp consolidating task** "📨 SEND DESK READY — 4 verified changeby pitches" (created this run; id recorded in the ClickUp comment thread + final message).
Embedded (not counted separately): the verified 4-org contact dataset; the git commit that persists this handoff in version control.

## 6. ACTIONABLE-ASSET CONFIRMATION (evidence + links)
- **ClickUp changeby pipeline (901417511645)** — all actionable items are on the board, link-verified (https://app.clickup.com/t/<id>):
  - 86bam5jmy "Send tonight: 6 Rochester pitch drafts (staged in Gmail)" — carries the Send Desk comment.
  - 86bak499e RHR→Mai Abdullah · 86bak49b7 RRRS · 86bak49dw Mary's Place→Charlsey Bickett · 86bak49gb KOP→Ellen Smith (the four targets).
  - 86bakk1kw "PUBLISH $49 toolkit" (the contradiction to resolve) · 86bakfhcp "evidence-based pricing $300/$600/$800".
  - Plus the new consolidating "SEND DESK READY" task created this run.
- **TickTick:** N/A this thread — no personal-development assets were produced; every item here is revenue-generating and therefore lives in ClickUp (correct per the ClickUp=revenue / TickTick=SOP split).
- **GitHub:** this handoff is committed to the local repo (`kimi/workspace`, .git present). Remote = `github.com/mozartg/youtube-content-pipeline.git` — **likely a misnamed home** (rx.md flagged the same). Push status recorded in the final message; if push needs auth, the file is committed locally and one `git push` completes it. Only this handoff file was staged — the workspace's untracked secrets (e.g., `.telegram_bot_state.json`) were deliberately NOT added.

## 7. OPEN LOOPS / LAST-MILE (in priority order)
1. **Send the 4 verified changeby pitches** — fix "Sadiya"→"Mai" first; attach the Readiness-Audit leave-behind; Tue–Thu 8–10am.
2. Get RRRS's email (call 585-319-3134), then send pitch #4.
3. Decide the $49 toolkit: demote to order-bump (recommended), publish as-is, or shelve — and close/relabel task 86bakk1kw accordingly.
4. Confirm the correct GitHub repo (the current remote is a youtube-content repo); then push handoffs there.
5. Inherited from rx.md: import `sync/TickTick_SOPs.xlsx` into TickTick (no connector; manual).

## 8. REUSABLE HANDOFF PROTOCOL (adaptation — for ANY sunsetting thread)
Use this 8-part skeleton before a thread is deleted, so its contribution persists. Do NOT invent new systems — reuse `rx.md`, the `THREAD_HANDOFF_*` files, and this skeleton; write to the workspace (git) + Cowork outputs + Drive, and register actionable items on ClickUp.
(0) Proposed name (sub-identity inside the canon). (1) Reasoning style — behaviors, model-agnostic. (2) Self-test — a behavior checklist + one concrete probe + the countersign. (3) Canonical anchors — reuse, don't re-derive. (4) What the thread did — live state, evidence. (5) Asset glossary + count — names, locations, links. (6) Actionable-asset confirmation — every actionable item on ClickUp (or TickTick if personal-dev), with links; flag any gap honestly. (7) Open loops / last-mile, prioritized. (8) This protocol, so the next dying thread inherits it.
Persistence rule: a handoff is only "comprehensive" if a fresh thread of any capability could resume the lane from these docs ALONE, with no access to the deleted conversation.

**Mo's last-mile action is:** send the 4 verified changeby pitches (fix "Sadiya"→"Mai" first); everything actionable is already on the ClickUp board (§6).

*Machine stages, Mozart places — ARBNET C1 online.*
